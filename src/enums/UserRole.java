package enums;

public enum UserRole {
    SELLER, // The role where the user can sell items on the platform
    BUYER, // The role where the user can buy, offer for, and make a wishlist of items on the platform

    ADMIN // The role where the user can manage pending items by accepting or rejecting them
}

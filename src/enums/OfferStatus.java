package enums;

public enum OfferStatus {
    PENDING, // Default status, when offer is newly created by buyer
    ACCEPTED, // When offer is accepted by seller
    DECLINED // When offer is declined by seller
}
